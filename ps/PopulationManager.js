class PopulationManger {
    constructor(ps, stats) {
        this.ps    = ps
        this.stats = stats
    }

    update(seconds) {
        const {
                  ps,
                  stats
              } = this
    }
}

module.exports = PopulationManger